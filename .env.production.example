# VPS production settings for docker compose.
APP_DOMAIN=ghostpoll.example.com
BASE_URL=https://ghostpoll.example.com

# Generate a strong value before deployment:
# openssl rand -hex 32
REDIS_PASSWORD=change-this-long-random-password
